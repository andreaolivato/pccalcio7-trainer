# Project rules for Claude

- **Never commit or push.** The owner handles all git commits and pushes themselves. Do not run `git commit`, `git push`, or anything that writes to git history, even if asked to "finish up" or "ship it".
